query FetchChatHistory {
  messages {
    user
    text
  }
}

mutation sendMessage($user: String!, $text: String!, $roomId: String!) {
    postMessage(user: $user, text: $text, roomId: $roomId)
}

subscription ListenToChat {
  messages {
    user
    text
  }
}

