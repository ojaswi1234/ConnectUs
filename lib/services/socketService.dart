import 'package:flutter/material.dart';

class SocketService extends StatelessWidget {
  const SocketService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
